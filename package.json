{
  "name": "tokai-hanabi-map",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/prerender.mjs",
    "preview": "vite preview",
    "deploy": "npm run build && cd dist && touch .nojekyll && git init -b gh-pages && git add -A && git commit -m Deploy && git push -f https://github.com/Koya864/tokai-hanabi-map.git gh-pages && cd .. && rm -rf dist/.git"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "sharp": "^0.35.3",
    "vite": "^5.4.11"
  }
}
